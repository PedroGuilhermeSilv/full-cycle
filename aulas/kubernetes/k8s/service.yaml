apiVersion: v1
kind: Service
metadata:
  name: goserver-service
spec:
  selector:
    app: goservers
  ports:
    - name: goserver-service
      port: 80
      protocol: TCP
      targetPort: 8000
  type: ClusterIP