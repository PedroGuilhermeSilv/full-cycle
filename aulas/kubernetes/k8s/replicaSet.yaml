apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: go-server
  labels:
    app: goservers
spec:
  selector:
    matchLabels:
      app: goservers
  replicas: 2
  template:
    metadata:
      labels:
        app: "goservers"
    spec:
      containers:
        - name: goservers
          image: "guilhermesilv/hello-go:v2"